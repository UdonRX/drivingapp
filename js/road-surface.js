import{state,roadPoint}from'./state.js';
function quad(ctx,x1,y1,x2,y2,x3,y3,x4,y4,c){ctx.fillStyle=c;ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.lineTo(x3,y3);ctx.lineTo(x4,y4);ctx.closePath();ctx.fill()}
export function drawRoad(ctx,w,h,hor){const slices=state.fpsSmoother<43?54:72;for(let i=0;i<slices;i++){const p1=roadPoint(i/slices,w,h,hor),p2=roadPoint((i+1)/slices,w,h,hor),alt=Math.floor(state.roadPhase*.07+i*1.2)%2===0,road=state.time==='night'?(alt?'#15191e':'#181d22'):(alt?'#31363a':'#353a3e'),shoulder=state.environment==='coast'?'#8c8580':state.environment==='city'?'#4b4e50':'#64635b';quad(ctx,0,p1.y,p1.x-p1.halfWidth,p1.y,p2.x-p2.halfWidth,p2.y,0,p2.y,shoulder);quad(ctx,p1.x+p1.halfWidth,p1.y,w,p1.y,w,p2.y,p2.x+p2.halfWidth,p2.y,shoulder);quad(ctx,p1.x-p1.halfWidth,p1.y,p1.x+p1.halfWidth,p1.y,p2.x+p2.halfWidth,p2.y,p2.x-p2.halfWidth,p2.y,road);if(alt){const e1=Math.max(1,p1.halfWidth*.025),e2=Math.max(1,p2.halfWidth*.025);quad(ctx,p1.x-p1.halfWidth,p1.y,p1.x-p1.halfWidth+e1,p1.y,p2.x-p2.halfWidth+e2,p2.y,p2.x-p2.halfWidth,p2.y,'#e9ece9');quad(ctx,p1.x+p1.halfWidth-e1,p1.y,p1.x+p1.halfWidth,p1.y,p2.x+p2.halfWidth,p2.y,p2.x+p2.halfWidth-e2,p2.y,'#e9ece9')}const dash=Math.floor(state.roadPhase*.12+i*2.4)%12;if(dash<6&&i>6)for(const lane of[-1,1]){const lx1=p1.x+p1.halfWidth*lane/3,lx2=p2.x+p2.halfWidth*lane/3,lw1=Math.max(.6,p1.halfWidth*.008),lw2=Math.max(.8,p2.halfWidth*.008);quad(ctx,lx1-lw1,p1.y,lx1+lw1,p1.y,lx2+lw2,p2.y,lx2-lw2,p2.y,'rgba(245,245,238,.88)')}}if(state.weather==='rain'){const p=roadPoint(.78,w,h,hor),g=ctx.createLinearGradient(0,p.y,0,h);g.addColorStop(0,'rgba(180,210,225,0)');g.addColorStop(1,state.time==='night'?'rgba(120,170,205,.22)':'rgba(210,230,238,.18)');ctx.fillStyle=g;ctx.beginPath();ctx.moveTo(p.x-p.halfWidth,p.y);ctx.lineTo(p.x+p.halfWidth,p.y);ctx.lineTo(w,h);ctx.lineTo(0,h);ctx.closePath();ctx.fill()}}
